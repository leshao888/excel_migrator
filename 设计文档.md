# Excel 数据迁移工具 - 设计文档

## 1. 项目概述

### 1.1 项目背景
提供一种可视化的 Excel 数据迁移工具，支持将数据源 Excel 中的指定范围数据映射到模板 Excel 的对应位置，实现批量、高效的数据迁移。

### 1.2 核心功能
- Excel 文件管理（上传、选择、更新、删除）
- Sheet 名称自动匹配（支持模糊匹配）
- 单元格范围映射配置（支持多种映射方向）
- 数据迁移执行（空则写，跳过非空）
- 映射配置导入/导出
- 批量数据源/模板管理
- 批量迁移（按映射配置自动匹配）

### 1.3 设计原则
- **可扩展**：映射策略、写入模式、存储方式均抽象为接口，支持插件式扩展
- **可维护**：模块职责单一，配置与代码分离
- **用户友好**：提供可视化界面，支持文件锁定检测、文件不存在提示

### 1.3 技术栈
- Python 3.10+
- Streamlit（Web UI）
- openpyxl（Excel 操作）
- JSON（配置存储）

---

## 2. 系统架构

### 2.1 目录结构

```
excel_migrator/
├── core/                          # 核心业务逻辑
│   ├── __init__.py
│   ├── models.py                  # 数据模型定义
│   ├── copier.py                   # 数据复制核心引擎
│   ├── matcher.py                  # Sheet 匹配器（可扩展）
│   ├── validators/                 # 验证器模块（可扩展）
│   │   ├── __init__.py
│   │   ├── mapping_validator.py    # 映射配置验证
│   │   └── file_validator.py       # 文件有效性验证
│   └── enums.py                    # 枚举定义
├── storage/                        # 持久化存储
│   ├── __init__.py
│   ├── store.py                    # 存储管理器
│   └── adapters/                   # 存储适配器（可扩展）
│       ├── __init__.py
│       └── json_adapter.py         # JSON 存储适配器
├── ui/                             # 用户界面
│   ├── __init__.py
│   ├── pages/                      # 页面模块
│   │   ├── __init__.py
│   │   ├── data_source_page.py     # 数据源管理
│   │   ├── template_page.py        # 模板管理
│   │   ├── mapping_page.py         # 映射配置
│   │   └── migrate_page.py         # 执行迁移
│   └── components.py               # 公共组件
├── utils/                          # 工具函数
│   ├── __init__.py
│   ├── excel_utils.py              # Excel 工具函数
│   └── path_utils.py               # 路径工具函数
├── config.py                       # 全局配置
├── main.py                         # 应用入口
└── requirements.txt                # 依赖
```

### 2.2 模块职责

| 模块 | 职责 | 可扩展点 |
|------|------|----------|
| `core/models` | 定义数据模型（CopyRule, SheetMapping, DataSource 等） | 新增映射规则类型 |
| `core/copier` | 数据复制核心逻辑 | 新增写入策略（覆盖/追加） |
| `core/matcher` | Sheet 名称匹配逻辑 | 支持更多匹配算法 |
| `core/validators` | 验证映射配置、文件有效性 | 新增验证规则 |
| `storage` | 数据持久化（数据源/模板路径、映射配置） | 适配其他存储介质 |
| `ui/pages` | 各功能页面 | 新增页面 |
| `utils` | 公共工具函数 | 工具函数库 |

---

## 3. 数据模型

### 3.1 枚举定义 (`core/enums.py`)

```python
class Direction(Enum):
    """数据复制方向"""
    HORIZONTAL = "horizontal"   # 横向（向右）
    VERTICAL = "vertical"       # 纵向（向下）

class WriteMode(Enum):
    """写入模式"""
    SKIP_NONEMPTY = "skip_nonempty"   # 跳过非空单元格
    OVERWRITE = "overwrite"            # 覆盖（预留接口）

class MatchMode(Enum):
    """Sheet 匹配模式（可扩展）"""
    EXACT = "exact"           # 精确匹配
    FUZZY = "fuzzy"           # 模糊匹配
    MAPPING = "mapping"       # 用户自定义映射
```

### 3.2 核心模型 (`core/models.py`)

```python
@dataclass
class CellRange:
    """单元格范围"""
    start_cell: str           # 起始单元格，如 "A1"
    direction: Direction     # 方向
    length: int               # 长度

@dataclass
class CopyRule:
    """复制规则"""
    source_range: CellRange   # 数据源范围
    target_range: CellRange   # 目标范围
    direction: Direction       # 方向（可继承自配置默认值）
    length: int               # 长度

@dataclass
class SheetMapping:
    """Sheet 映射配置"""
    source_sheet: str         # 数据源 sheet 名称
    target_sheet: str         # 模板 sheet 名称
    copy_rule: CopyRule       # 复制规则

@dataclass
class MappingConfig:
    """完整映射配置"""
    version: str              # 配置版本
    default_direction: Direction
    default_write_mode: WriteMode
    sheet_mappings: list[SheetMapping]

    @classmethod
    def from_dict(cls, data: dict) -> "MappingConfig":
        """从字典构建配置（支持扩展新字段）"""
        ...
```

### 3.3 存储模型

```python
@dataclass
class DataSourceItem:
    """数据源条目"""
    id: str
    name: str
    file_path: str
    sheets: list[str]
    added_at: str

@dataclass
class TemplateItem:
    """模板条目"""
    id: str
    name: str
    file_path: str
    sheets: list[str]
    added_at: str

@dataclass
class MappingConfigItem:
    """映射配置条目"""
    id: str
    name: str
    config: MappingConfig
    created_at: str
    updated_at: str
```

---

## 4. 映射配置格式

### 4.1 JSON Schema

```json
{
  "version": "1.0",
  "default_direction": "horizontal",
  "default_write_mode": "skip_nonempty",
  "sheet_mappings": [
    {
      "data_source_sheet": "1、GVM 预测表",
      "template_file": "月度汇总模板.xlsx",
      "copy_rule": {
        "source_start": "A1",
        "target_start": "B3",
        "direction": "horizontal",
        "length": 12
      }
    }
  ]
}
```

### 4.2 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `version` | string | 是 | 配置版本号 |
| `default_direction` | string | 是 | 默认方向：horizontal / vertical |
| `default_write_mode` | string | 是 | 默认写入模式：skip_nonempty / overwrite |
| `sheet_mappings` | array | 是 | Sheet 映射列表 |
| `sheet_mappings[].data_source_sheet` | string | 是 | 数据源的 sheet 名称 |
| `sheet_mappings[].template_file` | string | 是 | 目标模板文件名称（如 "月度汇总模板.xlsx"） |
| `sheet_mappings[].copy_rule` | object | 是 | 复制规则 |
| `copy_rule.source_start` | string | 是 | 数据源起始单元格 |
| `copy_rule.target_start` | string | 是 | 模板起始单元格 |
| `copy_rule.direction` | string | 否 | 覆盖默认方向 |
| `copy_rule.length` | int | 是 | 复制长度 |

### 4.3 示例文件

工具提供示例 JSON 文件（标明"示例"），用户下载后填写，上传导入。

---

## 5. 核心功能设计

### 5.1 Sheet 匹配器 (`core/matcher.py`)

```python
class SheetMatcher(ABC):
    """Sheet 匹配器抽象基类（可扩展）"""

    @abstractmethod
    def match(self, source_sheet: str, target_sheet: str) -> bool:
        """判断 source_sheet 是否能匹配 target_sheet"""
        pass

class ExactMatcher(SheetMatcher):
    """精确匹配"""
    def match(self, source_sheet: str, target_sheet: str) -> bool:
        return source_sheet == target_sheet

class FuzzyMatcher(SheetMatcher):
    """模糊匹配（包含关系）"""
    def match(self, source_sheet: str, target_sheet: str) -> bool:
        return source_sheet in target_sheet or target_sheet in source_sheet

class MappingMatcher(SheetMatcher):
    """用户自定义映射匹配"""
    def __init__(self, mapping_dict: dict[str, str]):
        self._mapping = mapping_dict

    def match(self, source_sheet: str, target_sheet: str) -> bool:
        return self._mapping.get(source_sheet) == target_sheet
```

### 5.2 数据复制器 (`core/copier.py`)

```python
class DataCopier:
    """数据复制器"""

    def __init__(self, write_mode: WriteMode = WriteMode.SKIP_NONEMPTY):
        self._write_mode = write_mode

    def copy(
        self,
        source_wb: Workbook,
        target_wb: Workbook,
        mapping: SheetMapping
    ) -> CopyResult:
        """执行单个 sheet 的数据复制"""
        ...

    def _read_range(self, ws: Worksheet, start: str, direction: Direction, length: int) -> list[list]:
        """读取指定范围的数据"""
        ...

    def _write_range(self, ws: Worksheet, start: str, direction: Direction, length: int, data: list[list], write_mode: WriteMode):
        """写入数据到指定范围"""
        ...
```

### 5.3 验证器 (`core/validators/`)

```python
# mapping_validator.py
class MappingValidator(ABC):
    """映射验证器抽象基类"""

    @abstractmethod
    def validate(self, config: MappingConfig, source_wb: Workbook, target_wb: Workbook) -> list[ValidationError]:
        pass

class RangeValidator(MappingValidator):
    """范围验证器"""
    def validate(self, config: MappingConfig, source_wb: Workbook, target_wb: Workbook) -> list[ValidationError]:
        errors = []
        for mapping in config.sheet_mappings:
            # 验证 sheet 是否存在
            # 验证单元格范围是否有效
            # 验证长度是否匹配
            ...
        return errors
```

---

## 6. 存储设计

### 6.1 存储文件

| 文件 | 内容 | 路径 |
|------|------|------|
| `data_sources.json` | 数据源列表（名称、路径、sheets） | `./storage/data_sources.json` |
| `templates.json` | 模板列表 | `./storage/templates.json` |
| `mapping_configs.json` | 映射配置列表 | `./storage/mapping_configs.json` |

### 6.2 JSON 结构

**data_sources.json**
```json
{
  "items": [
    {
      "id": "uuid-string",
      "name": "2026年销售数据",
      "file_path": "C:/Users/xxx/data.xlsx",
      "sheets": ["1、GVM预测表", "订单明细"],
      "added_at": "2026-04-10T10:00:00"
    }
  ]
}
```

**templates.json**
```json
{
  "items": [
    {
      "id": "uuid-string",
      "name": "月度汇总模板",
      "file_path": "C:/Users/xxx/template.xlsx",
      "sheets": ["GLM_GMV预测表", "汇总表"],
      "added_at": "2026-04-10T10:00:00"
    }
  ]
}
```

**mapping_configs.json**
```json
{
  "items": [
    {
      "id": "uuid-string",
      "name": "GVM映射方案",
      "config": { ... },
      "created_at": "2026-04-10T10:00:00",
      "updated_at": "2026-04-10T10:00:00"
    }
  ]
}
```

### 6.3 存储管理器 (`storage/store.py`)

```python
class StorageManager:
    """存储管理器"""

    def __init__(self, storage_dir: str = "./storage"):
        self._adapter = JsonStorageAdapter(storage_dir)

    def load_data_sources(self) -> list[DataSourceItem]:
        ...

    def save_data_source(self, item: DataSourceItem):
        ...

    def delete_data_source(self, id: str):
        ...

    # 同理 for templates, mapping_configs
```

---

## 7. UI 页面设计

### 7.1 页面概览

```
┌────────────────────────────────────────────────────────────┐
│                    Excel 数据迁移工具                        │
├──────────────┬──────────────┬──────────────┬──────────────┤
│  数据源管理   │   模板管理    │   映射配置    │   执行迁移    │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

### 7.2 数据源管理页面

**功能：**
- 新增数据源：上传 Excel 文件
- 选择已有数据源：下拉框
- 显示该数据源的 sheets 列表
- 更新数据源（重新上传/修改名称）
- 删除数据源

**布局：**
```
┌─────────────────────────────────────────────────────────┐
│ 新增数据源                                                │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  [上传 Excel 文件                    ] [上传]        │ │
│ └─────────────────────────────────────────────────────┘ │
│ 名称: [________________________]                          │
│                                                         │
│ 已有数据源: [下拉框 ──────────────────] [选择] [删除]     │
│                                                         │
│ Sheet 列表:                                             │
│ - 1、GVM 预测表                                          │
│ - 订单明细                                               │
└─────────────────────────────────────────────────────────┘
```

### 7.3 模板管理页面

**功能：**
- 新增模板：上传 Excel 文件
- 选择已有模板：下拉框
- 显示该模板的 sheets 列表
- 更新、删除模板

### 7.4 映射配置页面

**功能：**
- 下载示例 JSON 文件（标明"[示例]"）
- 导入映射配置 JSON
- 查看已导入的映射配置列表
- 查看映射配置详情
- 删除映射配置

**布局：**
```
┌─────────────────────────────────────────────────────────┐
│ 映射配置                                                  │
│                                                         │
│ [下载示例文件] ← 标明"[示例]GVM映射配置.json"             │
│                                                         │
│ 导入配置: [________________________] [选择文件] [导入]    │
│                                                         │
│ 已导入的配置:                                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [下拉框：选择配置]                      [查看] [删除] │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ 配置详情: (JSON 预览)                                    │
│ {                                                      │
│   "version": "1.0",                                     │
│   "sheet_mappings": [...]                              │
│ }                                                      │
└─────────────────────────────────────────────────────────┘
```

### 7.5 执行迁移页面

**功能：**
- 选择数据源（下拉框）
- 选择模板（下拉框）
- 选择映射配置（下拉框）
- 执行迁移按钮
- 结果展示区

**布局：**
```
┌─────────────────────────────────────────────────────────┐
│ 执行迁移                                                  │
│                                                         │
│ 数据源: [下拉框 ──────────────────]                      │
│ 模板:   [下拉框 ──────────────────]                      │
│ 映射配置: [下拉框 ──────────────────]                    │
│                                                         │
│ [执行迁移]                                               │
│                                                         │
│ ─────────────────────────────────────────────────────  │
│ 迁移结果                                                  │
│                                                         │
│ ✅ 成功: 1 个 Sheet                                      │
│    - 1、GVM 预测表 → GLM_GMV预测表 (A1→B3, 12格)         │
│                                                         │
│ ⚠️ 跳过: 1 个 Sheet                                      │
│    - 订单明细 → 模板中不存在 Sheet                       │
│                                                         │
│ ─────────────────────────────────────────────────────  │
│ 总计: 2 个 Sheet                                         │
│ 成功: 1 | 跳过: 1                                        │
└─────────────────────────────────────────────────────────┘
```

### 7.6 错误提示

| 场景 | 提示方式 |
|------|----------|
| 文件路径无效 | 红色警告: "文件不存在，请重新选择" |
| 文件被占用 | 红色警告: "文件已打开，请关闭后重试" |
| 只有数据源无模板 | 警告框: "仅选择了数据源，请同时选择模板" |
| 匹配上的 Sheet | 成功提示: "已匹配 N 个 Sheet" |
| 映射配置格式错误 | 红色警告 + 错误位置 |

---

## 8. 扩展性设计

### 8.1 映射方式扩展

当前支持：
- 单元格范围映射（范围 → 范围）

可扩展：
- 单值映射（单个单元格 → 单个单元格）
- 行映射（整行 → 整行）
- 列映射（整列 → 整列）

实现方式：新增 `MappingStrategy` 抽象基类，每种映射方式实现对应策略类。

### 8.2 写入模式扩展

当前支持：
- `skip_nonempty`: 跳过非空单元格

可扩展：
- `overwrite`: 直接覆盖
- `append`: 追加到末尾
- `conditional`: 条件写入

实现方式：新增 `WriteStrategy` 抽象基类，通过工厂模式注册。

### 8.3 Sheet 匹配器扩展

当前支持：
- 用户自定义映射字典

可扩展：
- 正则匹配
- AI 智能匹配（预留接口）

实现方式：新增 `SheetMatcher` 子类，配置化选择。

### 8.4 存储扩展

当前支持：
- JSON 文件存储

可扩展：
- SQLite 数据库
- 云存储

实现方式：新增 `StorageAdapter` 子类，替换 `JsonStorageAdapter`。

---

## 9. 项目规范

### 9.1 命名规范
- 类名: PascalCase（如 `DataCopier`）
- 函数名: snake_case（如 `load_data_sources`）
- 常量: UPPER_SNAKE_CASE（如 `DEFAULT_STORAGE_DIR`）

### 9.2 代码组织
- 每个模块单一职责
- 优先使用组合而非继承
- 接口与实现分离
- 配置与代码分离

### 9.3 错误处理
- 自定义异常类
- 统一错误码规范
- 错误信息国际化预留

---

## 10. 待确认事项

1. **映射配置示例文件** - 示例文件名是否标明"[示例]"，如 `[示例]映射配置.json`？

2. **迁移结果输出** - 迁移后的文件是直接修改模板，还是另存为新文件？

3. **批量操作** - 是否支持一次性执行多个数据源→模板的迁移？
